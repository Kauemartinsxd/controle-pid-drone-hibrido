\section{Robustness Study}
\label{sec:robustness}

The comparison of Section~\ref{sec:sil-comparison} holds for the nominal
plant --- the very model both controllers were designed on --- and on
that plant the PID wins on every performance metric. This section asks
how much of that picture survives when the plant is no longer the
design model. The protocol is deliberately strict: \emph{only the plant
changes}. Both controllers keep the gains and trim commands of the nominal
design, and every run repeats the combined mission of
Section~\ref{sec:sil-comparison} ($\VT$ $12\!\to\!15.2$\,m/s at 5\,s,
heading doublet $\pm5^\circ$ at 50/100/150\,s, altitude doublet
$\pm5$\,m at 80/160/240\,s, 300\,s total) with the same reference set
used throughout Section~\ref{sec:sil}. Four scenarios are considered,
spanning plausible-to-severe model uncertainty and an exogenous
disturbance: (A)~a severe model mismatch, replacing the complete
aerodynamic set by an independent identification of the same airframe;
(B)~the longitudinal half of that mismatch only, to isolate the origin of
the failure observed in~(A); (C)~a plausible identification uncertainty,
multiplying all 23 stability and control derivatives by 1.10; and (D)~wind
and gusts applied through the native disturbance inputs
of~\eqref{eq:inputvec}. Table~\ref{tab:robustness} summarizes the mission
tracking errors --- the degradation columns normalize each scenario by the
same controller's nominal performance, so that the two laws are compared
against themselves rather than against each other --- and
Table~\ref{tab:lqry-rob} tracks the LQRy's requirements scorecard across
the scenarios.

\begin{table*}[!t]
\caption{Robustness campaign: RMS tracking errors ($t>12$\,s) and
degradation relative to the nominal scenario (RMS scenario / RMS nominal).
Gains and trim always from the nominal design.}
\label{tab:robustness}
\centering
\begin{tabular}{llcccccc}
\toprule
 & & \multicolumn{3}{c}{RMS error} & \multicolumn{3}{c}{Degradation ($\times$ nominal)} \\
Scenario & Ctrl. & $\VT$ [m/s] & $\psi$ [$^\circ$] & $h$ [m] & $\VT$ & $\psi$ & $h$ \\
\midrule
Nominal & PID & 0.071 & 1.27 & 1.65 & --- & --- & --- \\
 & LQRy & 0.38 & 2.92 & 1.78 & --- & --- & --- \\
\addlinespace
(A) Severe mismatch & PID & \multicolumn{6}{c}{loses the mission\textsuperscript{a}} \\
 & LQRy & 0.353 & 2.61 & 1.77 & 0.93 & 0.89 & 0.99 \\
\addlinespace
(B) Longitudinal only & PID & 0.081 & 1.28 & 113.8 & 1.14 & 1.01 & 68.9 \\
 & LQRy & 0.353 & 2.94 & 1.77 & 0.93 & 1.01 & 0.99 \\
\addlinespace
(C) All derivatives $\times1.10$ & PID & 0.073 & 1.28 & 1.65 & 1.03 & 1.01 & 1.00 \\
 & LQRy & 0.375 & 2.93 & 1.77 & 0.99 & 1.00 & 1.00 \\
\addlinespace
(D) Wind and gusts & PID & 0.104 & 2.24 & 2.55 & 1.46 & 1.76 & 1.54 \\
 & LQRy & 0.481 & 3.68 & 5.21 & 1.26 & 1.26 & 2.93 \\
\bottomrule
\multicolumn{8}{l}{\footnotesize\textsuperscript{a}Violent bounded
oscillation triggered by the heading doublet: RMS errors of}\\
\multicolumn{8}{l}{\footnotesize\phantom{\textsuperscript{a}}3.14\,m/s,
$818.7^\circ$, and 554.0\,m ($44\times$/$644\times$/$335\times$
nominal); see Section~\ref{sec:rob-sato}.}
\end{tabular}
\end{table*}

\begin{table}[!t]
\caption{LQRy requirements scorecard across the robustness scenarios
(mission reference events, $\VT$/$\psi$/$h$). The nominal row repeats
Table~\ref{tab:compliance}.}
\label{tab:lqry-rob}
\centering
\resizebox{\linewidth}{!}{%
\begin{tabular}{lccc}
\toprule
Scenario & OS [\%] & $t_s$ [s] & $e_{ss}$ [\%] \\
\midrule
Nominal & 16.5 / 3.4 / 0 & 34 / $\approx$50 / 41 & 0.1 / 3.1 / 0.1 \\
(A) Severe mismatch & 15.8 / 0 / 0 & 31 / $\approx$50 / 40 & 1.1 / 6.5 / 0.1 \\
(B) Longitudinal only & 15.8 / 5.1 / 0 & 31 / $\approx$50 / 40 & 1.1 / 4.7 / 0.2 \\
(C) $\times1.10$ & 12.5 / 3.8 / 0 & 35 / $\approx$50 / 41 & 0.9 / 3.5 / 0.1 \\
(D) Wind and gusts & 42.8 / 72.8 / 0 & 45 / $\approx$50 / $>$80 & 15.1 / 15.2 / $\gg$ \\
\bottomrule
\end{tabular}}
\end{table}

\subsection{Severe Model Mismatch}
\label{sec:rob-sato}
The perturbed plant replaces the nominal, flight-test-identified
aerodynamic set~\cite{angelo2026} by the complete alternative set
originally distributed with the nonlinear model
implementation~\cite{ferreira2019modeling,repo}, obtained through an
independent identification of the platform. The two sets differ
substantially --- the pitch-damping derivative $C_{m_q}$ by a factor of
4, the elevator effectiveness $C_{m_{\delta_e}}$ by 3.3, and the
lateral--directional set even changes the \emph{sign} of $C_{y_\beta}$
and $C_{y_p}$ --- so this scenario probes well beyond parametric
uncertainty: it is a structurally different aerodynamic description.

This is the scenario that inverts the nominal verdict. The PID ---
the faster, tighter law of Section~\ref{sec:sil-comparison} ---
\emph{loses the mission}: the heading doublet triggers a violent
bounded oscillation, the heading error grows to ${\sim}2100^\circ$
(the aircraft flies repeated full circles), pitch swings of tens of
degrees appear, and the altitude sinks by hundreds of meters
(Table~\ref{tab:robustness}: RMS heading error of $819^\circ$,
altitude error of 554\,m). The fixed-gain LQRy, on the very same
perturbed plant, \emph{completes the mission with near-nominal error}:
degradation factors of $0.93$--$0.99$ on every guidance channel
(Fig.~\ref{fig:rob-sato}), and a requirements scorecard essentially
unchanged from the nominal one (Table~\ref{tab:lqry-rob}). A law that
concedes a factor of 5 in nominal airspeed RMS absorbs, without
retuning, a plant whose lateral derivatives changed sign --- the
robustness heritage classically associated with LQ
designs~\cite{safonov1977gain}.

That heritage, however, is not a theorem here: the guaranteed margins
of LQ \emph{state} feedback do not survive output feedback in
general~\cite{doyle1978guaranteed}, so the tolerance observed is an
empirical property of this delivery --- earned by its deliberately
soft tuning --- rather than a structural guarantee.
\begin{figure}[!t]\centering
\includegraphics[width=\linewidth]{fig_v1_rob_sato.png}
\caption{Scenario A (severe mismatch): combined mission on the
perturbed plant, nominal-design controllers. The PID enters a violent
bounded oscillation and loses the mission; the fixed-gain LQRy
completes it with near-nominal error.}
\label{fig:rob-sato}\end{figure}

\subsection{Isolating the Origin: Longitudinal Mismatch Only}
\label{sec:rob-satoL}
Scenario B replaces only the \emph{longitudinal} derivatives by the
mismatched set, keeping the nominal lateral--directional model. Both
laws now complete the mission, which identifies the
lateral--directional half of the mismatch --- with its sign inversions
--- as what defeats the PID in Scenario~A. The LQRy again stays near
nominal in every channel (Table~\ref{tab:lqry-rob}). The PID
maintains heading and airspeed at nominal levels (degradation of 1.01
and 1.14), but the longitudinal mismatch alone is not benign for it:
the altitude channel degrades by a factor of 69 (RMS 113.8\,m, drift
of $+193$\,m), a steady drift of the altitude hold. The mechanism is
specific: the $\theta^{ref}$ clamp of $\pm10^\circ$
(Section~\ref{sec:pid}), sized for the control authority of the
nominal model, leaves the altitude loop without enough authority on
the perturbed plant, and the loop drifts without ever diverging.
\begin{figure}[!t]\centering
\includegraphics[width=\linewidth]{fig_v1_rob_satoL.png}
\caption{Scenario B (longitudinal mismatch only): the PID's altitude
hold drifts $+193$\,m while its heading and airspeed stay nominal;
the LQRy remains near nominal on every channel.}
\label{fig:rob-satoL}\end{figure}

\subsection{Plausible Identification Uncertainty}
\label{sec:rob-pert}
Scenario C multiplies all 23 stability and control derivatives by 1.10
--- a uniform $+10\%$ multiplicative perturbation with preserved signs,
representative of a realistic identification error, in the spirit of
the perturbed-derivative robustness campaigns of the fixed-wing UAV
literature~\cite{safwat2019design}. Both controllers are
insensitive to it: every PID degradation factor lies between 1.00 and
1.03, the LQRy scorecard is essentially the nominal one
(Table~\ref{tab:lqry-rob}), and the responses are visually
indistinguishable from the nominal runs. Within plausible
identification uncertainty, \emph{both} designs are robust; the
contrast between them only appears under the severe, sign-changing
mismatch of Scenarios A--B.

\subsection{Wind and Gusts}
\label{sec:rob-vento}
Small, low-speed fixed-wing UAVs are disproportionately sensitive to
atmospheric disturbance --- gust velocities are a large fraction of
their airspeed~\cite{mohamed2014fixedwing} --- so no comparison of
flight controllers for this class is complete without a wind scenario.
Scenario D perturbs the nominal plant through the wind inputs
of~\eqref{eq:inputvec}: a 4\,m/s tailwind ramped in over 20--25\,s, two
lateral $1-\cos$ gusts of $\pm3$\,m/s (55--65\,s and 105--115\,s, timed
over the heading doublet), and two vertical $1-\cos$ gusts of
$\pm2$\,m/s (85--95\,s and 165--175\,s, timed over the altitude
doublet). The $1-\cos$ profile is the classical discrete-gust
idealization~\cite{hoblit1988gust}, retained to this day in the gust
conditions of certification regulations~\cite{far25341}; deterministic
discrete gusts (rather than stochastic Dryden or von K\'arm\'an
turbulence~\cite{milf8785c1980,milhdbk1797}) keep the scenario exactly
reproducible run to run, consistent with the deterministic-bench
philosophy of Section~\ref{sec:hil}. Both controllers remain stable
and complete the mission with moderate peak errors --- and this is the
one disturbance class where the nominal ranking \emph{persists}
rather than inverts. The PID degrades most in heading under the
lateral gusts (RMS $\times1.76$, peak error $12.9^\circ$) but stays
firm elsewhere. The LQRy feels the wind more on both of its weaker
channels: heading peaks of $18.6^\circ$ and an altitude degradation of
$2.9\times$ its nominal RMS (${\approx}5.2$\,m, peak 11.2\,m), with
the gust transients sweeping its scorecard readings up accordingly
(overshoot entries of $42.8\%$/$72.8\%$ on $\VT$/$\psi$ in
Table~\ref{tab:lqry-rob} are gust excursions measured against the
same reference events, not capture overshoots). Neither controller
approaches loss of control.
\begin{figure}[!t]\centering
\includegraphics[width=\linewidth]{fig_v1_rob_vento.png}
\caption{Scenario D (wind and gusts): wind profile (NED components)
and closed-loop responses.}
\label{fig:rob-vento}\end{figure}

\subsection{Discussion}
\label{sec:rob-discussion}
The four scenarios, read together, draw the central trade-off of this
article. On the nominal plant the aggressively tuned cascade PID
dominates every performance metric (Section~\ref{sec:sil-comparison});
under the severe, sign-changing mismatch it is the PID that loses the
mission --- its fixed loop shapes invalidated by the inverted lateral
derivatives --- while the soft fixed-gain LQRy absorbs the same plant
change with degradation factors below unity on two channels. The
longitudinal-only mismatch adds a subtler failure mode on the PID
side: an outer loop silently starved by a clamp sized for the nominal
plant, drifting without diverging. Under wind the ranking reverts to
the nominal one --- the PID firmer on every channel, the LQRy
conceding altitude and heading peaks --- and within plausible
identification uncertainty both laws are robust. Robustness to severe
mismatch is not a free property of the LQ structure --- the classical
margins of LQ state feedback~\cite{safonov1977gain} are lost under
output feedback in general~\cite{doyle1978guaranteed} --- so the
tolerance observed here is an empirical property of this deliberately
soft delivery.
What the comparison isolates is therefore a genuine
performance--robustness trade-off between the two \emph{standard}
laws as delivered: the cascade PID converts its bandwidth into
nominal tracking quality; the fixed-gain LQRy converts its softness
into tolerance of model error. Robust-control syntheses that shape
this trade-off explicitly have been pursued for small fixed-wing UAVs
in our own research context~\cite{abdulhamid2016robusto}; here the
point is narrower: which of the two standard laws is preferable
depends on whether the dominant threat is model error --- where the
PID's failure is catastrophic and the LQRy's tolerance is decisive
--- or atmospheric disturbance, where the PID holds the line more
tightly.
